/**
 * Configuration
 */
var config = {

    database: {

    },

    notiwire: {
        api: '/api/' + 'v1',
        port: '1337'
    },

    logging: {
        path: 'logs/notiwire.log'
    }
}

module.exports = config;
